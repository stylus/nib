/*
 * Overflow utility.
 *
 * Synopsis:
 *
 *   overflow: <type>
 *
 * Examples:
 *
 *     overflow: auto
 *     overflow: hidden
 *     overflow: ellipsis
 *
 */

overflow(value)
  if value == 'ellipsis'
    overflow: hidden
    white-space: nowrap
    vendor('text-overflow', ellipsis, only: o ms official)
  else
    overflow: value
